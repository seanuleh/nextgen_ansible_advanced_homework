- hosts: workstation
  gather_facts: false
  tasks:
  - name: OpenStack servers
    os_server_info:
      cloud: openstack
      server: frontend
    register: result

  - name: Debug Info
    debug:
      msg: "IP is: {{ result }}"

  
  # - name: Curl website
  #   uri:
  #     url: "http://{{ result | json_query(domain.server[?name=='frontend'].accessIPv4) }}"
  #     method: GET
  #   register: webpage

  # - name: Fail if 'Ansible has done its job' is not in the page content
  #   fail:
  #   when: "'Ansible has done its job' not in webpage.results[0].content"
  #   tags:
  #     - osp.smoke
